import modules;
import util;
import pos;

final class UnitType {
    string name;
    int x;
}

struct Unit {
    UnitType type;

    UnitPos pos;

    void tick(PathModule blerp) {
            assert (false);
    }
}

